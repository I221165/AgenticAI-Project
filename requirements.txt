# === Core Framework ===
pydantic>=2.0.0
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.10
langgraph>=0.0.20
fastapi>=0.100.0
uvicorn>=0.20.0
python-dotenv>=1.0.0
langchain-groq>=0.1.0

# === State Management ===
pymongo>=4.6.0      # MongoDB for state storage

# === Phase 2: Audio ===
elevenlabs>=1.2.2
pydub>=0.25.1
audioop-lts>=0.2.1  # Python 3.13 compatibility shim for pydub
gTTS>=2.5.0         # Free Google TTS (basic quality)
edge-tts>=7.0.0     # Free Microsoft Neural TTS (high quality) ⭐ RECOMMENDED
# TTS (Coqui) — requires Python <3.12. Install manually if using local TTS:
#   pip install TTS

# === Phase 3: Image + Video ===
Pillow>=10.0.0          # Image manipulation (brightness, filters, sepia/noir)
numpy>=1.26.0           # Pixel-level filter math
requests>=2.31.0        # Pollinations.ai HTTP image generation

# === Testing ===
pytest>=8.0.0
pytest-mock>=3.12.0     # unittest.mock integration for pytest

# === Utilities ===
typing_extensions>=4.0.0
aiofiles>=23.0.0        # Async file I/O for FastAPI static file serving
